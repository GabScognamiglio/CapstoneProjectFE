export interface Income {
        accountId: number,
        amount: number,
        tag: string,
        comment: string,
        date: Date,
        category: string,
        isRecurring: boolean
}
