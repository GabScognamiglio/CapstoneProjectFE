export interface Expense {
        accountId: number,
        amount: number,
        tag: string,
        comment: string,
        date: Date,
        category: string,
        isRecurring: boolean
    
}
